# Claude + Codex 完整协作工作流

## 🔄 完整工作流程

### 阶段 1: Claude 编写代码

**Claude (Max 版本)** 作为主力编码:
- 规划项目架构
- 编写核心代码
- 实现功能模块

```bash
# Claude 工作中...
# 创建组件、API、类型定义等
```

### 阶段 2: 启动 Codex 审阅

**后台启动 Codex (Plus 版本)** 审阅:

```bash
cd your-project && codex exec \
  --dangerously-bypass-approvals-and-sandbox \
  --skip-git-repo-check \
  --output-last-message "local/review_output.txt" \
  "审阅指令..." &
```

**关键点**: 使用 `&` 后台运行,Claude 可以继续编写下一个模块

### 阶段 3: Claude 继续工作

Claude **不等待** Codex 完成,继续编写其他代码:
- 并行工作,效率最大化
- Codex 在后台审阅,Claude 在前台编码

### 阶段 4: 查看 Codex 审阅结果

Codex 完成后,Claude 读取审阅结果:

```bash
cat local/review_output.txt
```

### 阶段 5: ⭐ Claude 分析并采纳建议

**这是最关键的一步!**

Claude 收到 Codex 审阅后:

#### 5.1 分析建议的优先级

Codex 通常会分类建议:
- **高优先级**: 必须修复(语法错误、类型错误、安全问题)
- **中优先级**: 应该修复(性能问题、最佳实践)
- **低优先级**: 可选优化(代码风格、注释改进)

#### 5.2 决定采纳方案

Claude 需要**独立思考**并决定:

✅ **采纳的建议** (示例):
- 修复错误处理不一致
- 优化 Table rowKey 使用稳定字段
- 添加 TypeScript 类型定义
- 改进可访问性(a11y)

⚠️ **暂不采纳的建议** (示例):
- 大规模重构(风险高)
- 文件编码问题(需要编辑器层面解决)
- 过度优化(投入产出比不高)

#### 5.3 思考并修复代码

Claude **直接修复**采纳的建议:

**示例 1: 修复错误处理**
```typescript
// Codex 建议: 保留 AxiosError 结构
// Claude 采纳并修复:

// api.ts - 修改前
return Promise.reject(new Error(errorMessage));

// api.ts - 修改后
return Promise.reject(error); // 保留原始 AxiosError

// HomePage.tsx - 修改前
catch (error: any) {
  message.error(error.response?.data?.detail || '...');
}

// HomePage.tsx - 修改后
catch (err) {
  const axiosErr = err as AxiosError<ApiResponse>;
  const errorMsg = axiosErr.response?.data?.message || ...;
  message.error(errorMsg);
}
```

**示例 2: 优化 rowKey**
```typescript
// Codex 建议: 避免使用 index 作为 rowKey
// Claude 采纳并修复:

// 修改前
<Table rowKey={(record, index) => `risk-${index}`} />

// 修改后
<Table rowKey={(record) => `${record.type}-${record.severity}-${record.description.substring(0, 20)}`} />
```

#### 5.4 记录修复内容

Claude 在修复后应该:
1. 更新项目文档
2. 记录采纳了哪些建议
3. 说明暂未采纳的原因

### 阶段 6: 迭代(可选)

如果代码有重大修改,可以再次请 Codex 审阅:

```bash
codex exec --dangerously-bypass-approvals-and-sandbox \
  --skip-git-repo-check \
  --output-last-message "local/review_v2.txt" \
  "re-review after fixes..." &
```

## 📋 完整流程图

```
┌─────────────────────────────────────────────────┐
│  Claude: 编写代码 (组件 A)                        │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  启动 Codex 审阅(后台运行) &                       │
└─────────────────┬───────────────────────────────┘
                  │
                  ├─────────────────────────────┐
                  │                             │
                  ▼                             ▼
┌───────────────────────────┐    ┌───────────────────────────┐
│  Claude: 继续编写代码      │    │  Codex: 后台审阅中...      │
│  (组件 B, 组件 C...)       │    │                           │
└───────────────┬───────────┘    └──────────┬────────────────┘
                │                            │
                │                            │
                │  <── 等待 Codex 完成 ──│
                │                            │
                ▼                            ▼
┌─────────────────────────────────────────────────┐
│  Claude: 读取 Codex 审阅结果                      │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  ⭐ Claude: 分析建议 + 决定采纳方案              │
│  - 高优先级: 必须修复                            │
│  - 中优先级: 应该修复                            │
│  - 低优先级: 可选优化                            │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Claude: 思考并直接修复代码                       │
│  - 修复错误处理                                  │
│  - 优化 rowKey                                  │
│  - 改进类型定义                                  │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  完成! (或进入下一轮迭代)                         │
└─────────────────────────────────────────────────┘
```

## 🎯 核心原则

### 1. Claude 的主导权
- **Claude 决定采纳哪些建议**
- **Claude 评估修复的优先级**
- **Claude 独立思考,不盲目执行**

### 2. Codex 的定位
- **顾问角色,提供专业建议**
- **不是最终决策者**
- **建议质量高,但需要 Claude 筛选**

### 3. 并行工作原则
- **后台运行 Codex**
- **Claude 继续编码,不等待**
- **效率最大化**

### 4. 务实原则
- **高优先级问题必须修复**
- **中优先级问题权衡修复**
- **低优先级问题可暂缓**
- **避免过度优化**

## 💡 最佳实践

### ✅ 应该采纳的建议
- 语法错误、编译错误
- 类型安全问题
- 错误处理不一致
- 安全漏洞
- 明显的性能问题
- 可访问性(a11y)缺陷

### ⚠️ 需要评估的建议
- 架构重构(评估风险)
- 性能优化(评估收益)
- 代码风格调整(看重要性)
- 添加复杂功能(看需求)

### ❌ 可以暂不采纳的建议
- 过度设计
- 编辑器/工具层面的问题
- ROI 不高的优化
- 与项目方向不符的建议

## 📝 修复记录模板

创建 `local/codex_fixes.md`:

```markdown
# Codex 审阅建议采纳记录

## 审阅时间
2025-10-09

## 采纳的建议

### 高优先级 (已修复)
- [x] 统一错误处理 (api.ts + HomePage.tsx)
  - 修复: 保留 AxiosError 结构
  - 文件: services/api.ts:49, pages/HomePage.tsx:29

- [x] 优化 Table rowKey
  - 修复: 使用稳定字段组合
  - 文件: pages/AnalysisReport.tsx:149, 197

### 中优先级 (计划修复)
- [ ] 添加 Error Boundary
- [ ] 细化 Loading 状态

### 暂未采纳
- 文件编码乱码 (需编辑器层面解决)
- 完全重构类型系统 (风险较高,暂缓)
```

## 🔗 相关文档

- [QUICK_START.md](./QUICK_START.md) - 快速开始
- [USAGE_GUIDE.md](./USAGE_GUIDE.md) - 详细使用指南
- [UI_REVIEW_GUIDE.md](./UI_REVIEW_GUIDE.md) - UI 审阅指南

---

**记住**: Claude 拥有最终决策权,Codex 是顾问,不是老板!
